apiVersion: v1
kind: Service
metadata:
  name: secure-web-service
  namespace: security-lab
spec:
  type: ClusterIP
  selector:
    app: secure-web-app
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP